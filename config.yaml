baseurl: "/"
title: "Oleg Kovalov homepage"
theme: "PaperMod"
languageCode: "en-us"
defaultcontentlanguage: "en"

paginate: 20
canonifyurls: true

params:
  homeInfoParams:
    title: "Oleg Kovalov"
    content: "Senior Software Engineer"
  sectionPagesMenu: "main"

  socialIcons:
    - name: "email"
      url: "mailto:oleg@hey.com"
    - name: "github"
      url: "https://github.com/cristaloleg"
    - name: "telegram"
      url : "https://t.me/olegkovalov"
    - name: "twitter"
      url : "https://twitter.com/oleg_kovalov"
    - name: "linkedin"
      url : "https://www.linkedin.com/in/olegkovalov"

  menu:
    - name: "About"
      url: "/about"
    - name: "Blog"
      url : "/posts"
    - name: "Projects"
      url : "/projects"
    - name: "Links"
      url : "/links"

# weight == index

  # defaultTheme: auto
  author: "Oleg Kovalov"
  info: "Kek"
  description: "Oleg Kovalov Homepage"

  keywords: ["engineer", "blog", "developer", "personal"]
  avatarurl: "images/ava-sammy.jpg"

  favicon_32: "/img/favicon-32x32.png"
  favicon_16: "/img/favicon-16x16.png"

  hidecredits: false
  hidecopyright: false
  rtl: false

  math: true
  custom_css: ["css/custom.css"]

  # dir name of your main content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName: "posts"

  # ["orange", "blue", "red", "green", "pink"]
  # themeColor: "orange"

  # if you set this to 0, only submenu trigger will be visible
  # showMenuItems: 2

  # show selector to switch language
  # showLanguageSelector: false

  # set theme to full screen width
  fullWidthTheme: false

  # center theme with default width
  # centerTheme: false

  # set a custom favicon (default is a `themeColor` square)
  # favicon: "favicon.ico"

  # set post to show the last updated
  # If you use git, you can set `enableGitInfo` to `true` and then post will automatically get the last updated
  # showLastUpdated: false

  # Provide a string as a prefix for the last update date. By default, it looks like this: 2020-xx-xx [Updated: 2020-xx-xx] :: Author
  # updatedDatePrefix: "Updated"

  # set all headings to their default size (depending on browser settings)
  # it's set to `true` by default
  # oneHeadingSize: false
